import React from "react";

const Home = () => {
    return (
        <div className="h-screen w-screen flex items-center justify-center dark:(bg-gray-300 text-white)">
            <span>Welcome to strada!</span>
        </div>
    );
};

export default Home;
